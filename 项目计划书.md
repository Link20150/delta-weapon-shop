# 猛攻武器铺 · 项目计划书（轻量版）

> 版本：v0.1（草稿） ｜ 日期：2026-08-21 ｜ 定位：自用 + 朋友小圈子

---

## 1. 项目概述

做一个微信小程序「猛攻武器铺」，核心解决一件事：**让玩家快速找到、复制一套适合自己预算和玩法的改枪方案**。

不做大而全的社区平台，不做复杂算法，第一版目标是「能用、好看、够用」：

- 自己用：查代码、复制、收藏
- 朋友用：打开小程序就能抄作业
- 维护成本：单人可维护，数据改动不依赖开发

---

## 2. 目标用户与使用场景

| 场景 | 用户动作 |
|---|---|
| 不会改枪、刚进游戏 | 打开小程序 → 看每日推荐（新手低价方案）→ 复制 → 进游戏导入 |
| 预算有限 / 只用缴获枪 | 选「预算档位 + 偏好」→ 看推荐方案 → 复制，方案优先便宜好用的 |
| 换了新枪不会改 | 枪库搜索枪名 → 看该枪的低价方案 → 复制 |
| 自己改了好枪想存起来 | 粘贴改枪码到「我的代码」→ 标注枪名/配置名 → 随时复制 |

预期规模：**10~50 人**（主要是自己和朋友），不需要用户注册体系，不需要运营后台。

### 产品原则（重要）

应用是给**完全不懂改枪的新手**用的（例如只用缴获枪的朋友），因此：

1. **快**：从打开到拿到代码不超过三步，不让人研究
2. **省**：推荐以低预算、性价比方案优先，先能用，不追满改
3. **简单**：不出现配件术语堆砌，一句话说明「为什么推荐这把」
4. **错位**：不追求专业深度，只解决「今天用什么枪、怎么最快用上」

---

## 3. 功能范围

### 3.1 第一版（MVP）功能

1. **改枪码库**
   - 按枪名搜索（模糊）、按模式筛选（烽火地带 / 全面战场）、按预算档位筛选
   - 方案列表：枪名、配置名、预算档位、标签（压枪/腰射/架点/性价比）、代码
   - 一键复制改枪码
   - 详情页：显示方案备注、推荐理由（一句话）、价格参考
2. **每日推荐**
   - 首页置顶展示 3~5 套每日推荐方案，**以新手低价方案为主**（管理员手动挑选，可写进云数据库）
   - 顺带展示「今日密码」（游戏内兑换码，手动填写，有则显示）
3. **条件推荐（极简版）**
   - 表单仅 3 项：地图难度（普通/机密/绝密）、预算档位、偏好标签（模式固定烽火地带）
   - 按简单规则打分（标签匹配 + 预算匹配），返回 Top 5 方案
4. **我的**
   - 收藏列表（收藏的方案）
   - 我的代码（粘贴保存自己的改枪码，支持打标签）

### 3.2 第一版明确不做

- 用户注册 / 登录 / 头像昵称（自用不需要）
- 社区：评论、点赞、排行榜（朋友用不到）
- 实时市场价格接口（手动维护预算档位即可）
- 订阅消息推送（每日推荐手动看就行）
- 管理后台（直接用微信云开发控制台改数据）
- **生成游戏改枪码**（官方格式未公开，无法真正生成，只能库内复制）
- 联机同步、H5/App 多端

---

## 4. 页面结构

TabBar 三个页签：

```
┌ 推荐页（首页） ──────────────┐
│ 今日密码（有则显示）          │
│ 每日推荐 × 3~5（卡片）        │
│ 条件推荐入口 → 问卷页         │
└──────────────────────────────┘

┌ 枪库页 ──────────────────────┐
│ 搜索框（枪名）               │
│ 筛选：模式 / 预算档位        │
│ 方案卡片列表                 │
│  → 详情页：方案信息 + 复制    │
└──────────────────────────────┘

┌ 我的页 ──────────────────────┐
│ 我的收藏                     │
│ 我的代码（粘贴/管理/复制）    │
└──────────────────────────────┘
```

总计 **6 个页面**：推荐页、枪库页、方案详情页、条件推荐问卷页、我的页、我的代码页。

---

## 5. 技术方案

### 方案 A（推荐）：原生微信小程序 + 微信云开发

- 小程序：原生 WXML / WXSS / JS（不引入框架，最简单）
- 后端：微信云开发（云函数 + 云数据库 + 云存储）
- 数据：武器、改枪码、每日推荐直接放云数据库集合
- 数据维护：打开云开发控制台直接增删改，**不需要写管理后台**

优点：

- 零服务器、零域名、零备案（云开发模式无需配置 request 合法域名）
- 成本几乎为 0（免费额度足够几十人使用）
- 数据可以在线更新，不用每次发版

缺点：

- 依赖微信云开发（换个平台不能直接用）
- 需要申请小程序账号 + 开通云开发

### 方案 B（最简）：纯本地数据，无后端

- 所有数据打包成 JSON 文件放进小程序代码里
- 零云服务、零费用，代码量最小

缺点：

- **更新数据必须重新发版**（改一条改枪码就要提审一次）
- 无法保存收藏到云端（只能用本地缓存，换手机丢失）

### 选择建议

**选方案 A**。虽然方案 B 代码更少，但「改数据要发版」对经常更新代码库的小工具来说太痛苦；方案 A 只是多了几个云函数，开发成本多 1~2 天，但日常维护省心得多。

---

## 6. 数据设计

云数据库集合（4 个）：

### `guns`：武器字典（约 60 条）

```json
{
  "name": "K416",
  "category": "突击步枪",
  "caliber": "5.56",
  "mode": ["烽火地带", "全面战场"],
  "tags": ["新手友好", "全距离"]
}
```

### `codes`：改枪码库（第一版目标 200~500 条）

```json
{
  "gunName": "K416",
  "configName": "20W 新手性价比",
  "mode": "全面战场",
  "priceLevel": "低",          // 低 / 中 / 高 / 满改
  "tags": ["性价比", "压枪"],
  "code": "K416突击步枪-5116089348491576161",
  "reason": "瞄具、握把、枪口三件套，稳定不贵",
  "valid": true
}
```

### `daily`：每日推荐（3~5 条/天）

```json
{
  "date": "2026-08-21",
  "codeId": "xxx",
  "title": "今日主打：新手第一把满改 K416",
  "sort": 1
}
```

### `my_codes`：用户自己的代码（可选，先做本地缓存版）

```json
{
  "openid": "oXXXX",
  "gunName": "M7",
  "configName": "朋友给的",
  "code": "M7战斗步枪-烽火地带-6HNT8200...",
  "createdAt": 1755744000000
}
```

---

## 7. 数据来源与维护

| 数据 | 来源 | 维护方式 |
|---|---|---|
| 武器字典 | 游戏内图鉴 / 三角洲 Wiki | 一次性录入 |
| 改枪码 200~500 条 | 现有开源库（delta-query、delta-force-gunsmith、Awesome_Delta_Force、BRaysMK 前缀识别表）+ NGA / B站 / 微信群里收集 | 分批导入，之后每周手动补几条 |
| 预算档位 | 按代码库已有价格标注（低/中/高/满改） | 手动 |
| 今日密码 | 游戏内/群友分享 | 每天花 1 分钟填 |
| 每日推荐 | 从代码库里挑 3~5 条 | 每天花 2 分钟改一条记录 |

> 不做实时价格：对自用场景，档位（低/中/高/满改）足够，省掉 orzice API 的接入和维护成本。如果以后想要，接口是现成的，可以后置。

---

## 8. 推荐逻辑（简单规则，不写算法）

条件推荐就是一个**打分函数**（云函数内 50 行以内）：

1. 数据已固定为「烽火地带」
2. 按「预算档位」过滤（用户选的档位 ± 1 档都算，低价优先）
3. 按「偏好标签」打分：命中一个 +1 分（性价比/新手友好/压枪/架点）
4. 地图难度只影响展示文案（机密/绝密 → 推荐价格档位高的方案），不参与计算
5. 同分时**「新手友好」「性价比」标签优先**，按分数排序取 Top 5

每日推荐不写定时任务：每天手动在云数据库 `daily` 集合改日期即可，或做一个云函数「一键生成今日推荐」（从代码库按热度/标签随机挑）。

---

## 9. 开发排期（业余时间，约 4 周）

| 周次 | 任务 | 产出 |
|---|---|---|
| 第 1 周 | 注册小程序账号、开通云开发、搭页面骨架（3 个 Tab + 6 个页面空壳） | 可运行的空壳小程序 |
| 第 2 周 | 数据建设：武器字典 + 第一批改枪码（200 条）导入云数据库；枪库页搜索/筛选/复制完成 | 枪库可用 |
| 第 3 周 | 推荐页 + 每日推荐 + 条件推荐问卷 + 我的收藏 + 我的代码 | 全部核心功能 |
| 第 4 周 | 联调、真机测试、提审发布、朋友内测收集反馈 | 正式上线体验版 |

估算总工时：**30~40 小时**（不含数据收集，数据收集另计 10~15 小时）。

---

## 10. 成本预算

| 项目 | 费用 |
|---|---|
| 微信小程序注册（个人主体） | 免费 |
| 云开发免费额度 | 免费（超出后按量，预计每月 < 1 元） |
| 域名 / 服务器 | 不需要 |
| 合计 | **约 0 元/月** |

---

## 11. 合规与风险

1. **改枪码无法生成**：官方格式未公开，只能「库内复制」，第一版不做生成功能，避免误导。
2. **代码会失效**：新赛季后旧改枪码可能导入失败 → 列表提供「失效」标记（自己标记即可），数据维护时清理。
3. **审核风险**：涉及游戏的小程序审核较敏感，需要注意：
   - 描述定位为「非官方学习交流工具」
   - 不使用游戏官方 Logo/素材做宣传图，枪械图片尽量用文字卡片代替或标注来源
   - 不出现任何「辅助/外挂/代练」类字样
4. **个人主体限制**：个人小程序无支付、部分类目受限，本项目不涉及，无影响。

---

## 12. 验收标准（Done 的定义）

- [ ] 小程序在微信开发者工具中无报错，真机可运行
- [ ] 枪库可搜索、筛选、一键复制改枪码（复制后能在游戏导入界面粘贴）
- [ ] 每日推荐页正常展示当天 3~5 套方案
- [ ] 条件推荐：选模式+预算+偏好后返回 5 套方案
- [ ] 收藏、我的代码增删查复制可用
- [ ] 云开发控制台改一条数据后，小程序端刷新可见（验证免发版更新）
- [ ] 通过微信审核，体验版可分享给朋友

---

## 13. 待确认事项

1. 技术方案：**选 A（云开发）还是 B（纯本地 JSON）**？（建议 A）
2. 数据来源：是否同意先用现有开源改枪码库做种子数据（200 条起步）？
3. 小程序名称已定为「猛攻武器铺」（避开「三角洲」商标风险），如审核要求可再调整。
